server:
  applicationConnectors:
    - type: http
      port: ${APPLICATION_PORT:-4410}
  adminConnectors:
    - type: http
      port: ${ADMIN_PORT:-4111}

serviceProviderURI: ${SERVICE_PROVIDER_URI:-"http://localhost:2210"}

brokerURI: ${BROKER_URI:-"http://localhost:5510"}

usingServiceProvider: ${IS_USING_SERVICE_PROVIDER:-true}

redisURI: ${REDIS_URI:-"redis://192.168.1.4:6380"}

verifiableCredentialsURI: ${VERIFIABLE_CREDENTIALS_URI:-"http://localhost:3002"}

trustframeworkRP: ${TRUSTFRAMEWORK_RP:-"http://localhost:4410"}

rp: ${RP:-"dbs"}

contractedIdpURI: ${CONTRACTED_IDP_URI:-"http://localhost:3333/sign-up"}
